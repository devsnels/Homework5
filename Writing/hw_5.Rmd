---
title: "hw_5.Rmd"
author: "Group 1"
date: "November 5, 2018"
output: pdf_document
---

```{r part_3}

library(readr)
library(dplyr)
library(sf)
library(tigris)
library(forcats)
library(ggplot2)

chicago_homicides <- read_csv("../Data/homicide-data.csv") %>% 
  filter(city == "Chicago")

chicago_homicides <- chicago_homicides %>% 
  mutate(unsolved = disposition %in% c("Closed without arrest", 
                                       "Open/No arrest")) %>% 
  mutate(top_3_races = fct_lump(victim_race, n = 3))
  
chicago_subdiv <- tracts(state = "IL", cb = TRUE, class = "sf") %>% 
  filter(COUNTYFP == "031")

ggplot() +
  geom_sf(data = chicago_subdiv, fill = "lightgray") +
  geom_sf(data = chicago_homicides, aes(geometry = lat, lon)) +
  theme_dark() +
  scale_fill_viridis_c(name = "Race of Victims") +
  facet_wrap(~ unsolved)


  

```

```{r part_4}
```

```{r part_5}
```